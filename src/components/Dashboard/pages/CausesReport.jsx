// CausesReport uses the same implementation as Causes page
// Just importing and exporting the Causes component with a different name
import Causes from './Causes';

const CausesReport = () => {
  return <Causes />;
};

export default CausesReport;
